# shared

Cross-domain primitives and foundations:

- `ui`: reusable low-level components
- `lib`: generic helper libraries
- `api`: transport and API clients
- `config`, `constants`, `types`, `stores`, `hooks`, `utils`
